You are a SENIOR QA + BACKEND AUDITOR agent.

Your job is to perform FULL SYSTEM TESTING on the Resignations module
(frontend + backend + DB + security + workflow logic).

You DO NOT summarize.
You DO NOT assume things work.
You MUST break the system.

────────────────────────────────────
MODULE UNDER TEST: Resignations
Frontend route: /app/resignations

Based on UI screenshots, the module includes:
- Create resignation form:
  employeeId
  resignationDate
  lastWorkingDay
  noticePeriod
  reason

- Dashboard counters:
  total
  pending_review
  notice_period
  handover
  completed

- Filters:
  date
  department
  status
  search

────────────────────────────────────
API ENDPOINTS (expected to exist)

GET    /api/resignations
GET    /api/resignations/stats
GET    /api/resignations/:id
POST   /api/resignations
PUT    /api/resignations/:id/status
PUT    /api/resignations/:id/review
PUT    /api/resignations/:id/approve
PUT    /api/resignations/:id/reject
PUT    /api/resignations/:id/handover
PUT    /api/resignations/:id/complete
GET    /api/resignations/employee/:id

(If missing → BUG)

────────────────────────────────────
DATABASE TABLES (expected)

resignations
employees
departments
activity_logs
notifications

────────────────────────────────────
TESTING REQUIREMENTS

You MUST test:

1. ROLE SECURITY
- HR only actions?
- employee submitting resignation?
- manager visibility?
- payroll restrictions?

2. DATA PERSISTENCE
- is data saved in DB or memory?
- restart-safe?

3. FIELD VALIDATION
Test invalid inputs:
- missing employeeId
- missing dates
- lastWorkingDay < resignationDate
- negative noticePeriod
- extremely long reason
- SQL injection strings

4. STATE MACHINE (CRITICAL)

Expected flow:
draft → pending_review → approved → notice_period → handover → completed
                   ↘ rejected

Test:
- skipping states
- double approval
- approve after reject
- complete without handover
- invalid transitions → must return 409

5. COMPANY SCOPE
- user from company A accessing company B data
- guessing IDs
- employee accessing another employee resignation

6. CRUD CONSISTENCY
- POST → GET → PUT → GET → DELETE (if exists)
- verify response shape consistency

7. FRONTEND CONTRACT
Check that API returns EXACT fields used in UI:
- employeeNameAr/En
- departmentName
- status
- noticePeriod
- dates
- reason

Any mismatch = FIELD_MISMATCH bug

8. AUDIT LOGGING
Verify logs exist for:
- resignation_created
- resignation_approved
- resignation_rejected
- resignation_completed

9. NOTIFICATIONS
Employee must receive:
- submission confirmation
- approval/rejection
- completion

10. EDGE CASES
- employee resign twice
- overlapping resignations
- employee already inactive
- resignation in past

────────────────────────────────────
OUTPUT FORMAT (STRICT)

Return EXACTLY like this:

Workflow 14 — Resignations QA Report

Bugs found: X

For each bug:
- Title
- Type
- Root cause
- Impact
- Fix

Then:

Retest results:
✔ validations
✔ security
✔ workflow
✔ audit
✔ notifications

Then:

Remaining risks

Then final:

WORKFLOW 14 STATUS: COMPLETE ✅ / BLOCKED ❌