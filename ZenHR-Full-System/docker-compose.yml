services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: zenhr
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d zenhr"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      APP_VERSION: local-compose
      PORT: 3001
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/zenhr
      JWT_SECRET: change-this-compose-secret-at-least-32-characters
      CORS_ORIGINS: http://localhost:8080
      TRUST_PROXY: "true"
      UPLOADS_DIR: /app/uploads
      EXPORTS_DIR: /app/exports
      RUNTIME_STORE_ADAPTER: redis
      REDIS_URL: redis://redis:6379/0
      STORAGE_ADAPTER: local
    volumes:
      - uploads-data:/app/uploads
      - exports-data:/app/exports
    ports:
      - "3001:3001"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    depends_on:
      - api
    ports:
      - "8080:80"

volumes:
  postgres-data:
  redis-data:
  uploads-data:
  exports-data:
